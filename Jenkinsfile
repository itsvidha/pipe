pipeline {
    agent any 
    stages {
        stage ('build'){
            steps {
           
                    echo "Welcome to jenkins"
                    // error "testing retry"
                
                echo "After retry block"
            }
        }
    }
}
